<odoo>
    <data>
        <record id="security_scan_list_view" model="ir.ui.view">
            <field name="name">security.scan.list</field>
            <field name="model">security.scan</field>
            <field name="arch" type="xml">
                <list string="Security Scans">
                    <field name="name"/>
                    <field name="scan_date"/>
                    <field name="master_password_set"/>
                    <field name="https_enabled"/>
                    <field name="access_rules_defined"/>
                    <field name="log_file_present"/>
                    <field name="db_filter_set"/>
                    <field name="db_listing_disabled"/>
                </list>
            </field>
        </record>

        <record id="security_scan_form_view" model="ir.ui.view">
            <field name="name">security.scan.form</field>
            <field name="model">security.scan</field>
            <field name="arch" type="xml">
                <form string="Security Scan">
                    <header>
                        <button name="run_scan" string="Run Scan" type="object" class="oe_highlight"
                                 attrs="{'invisible': [('running_scan', '=', True)]}"/>
                        <field name="running_scan" invisible="1"/>
                        <field name="progress" widget="progressbar"
                                 attrs="{'invisible': [('running_scan', '=', False)]}"/>
                    </header>
                    <sheet>
                        <group>
                            <group>
                                <field name="name"/>
                                <field name="scan_date" readonly="1"/>
                            </group>
                        </group>
                        <group string="Security Checks">
                            <field name="master_password_set"/>
                            <field name="https_enabled"/>
                            <field name="access_rules_defined"/>
                            <field name="log_file_present"/>
                            <field name="db_filter_set"/>
                            <field name="db_listing_disabled"/>
                        </group>
                        <group string="Notes">
                            <field name="notes"/>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="security_scan_action" model="ir.actions.act_window">
            <field name="name">Security Scans</field>
            <field name="res_model">security.scan</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p>
                    This menu allows you to perform security scans on your Odoo instance.
                </p>
            </field>
        </record>

        <menuitem id="security_scan_menu_root" name="Security Scan" sequence="10"/>
        <menuitem id="security_scan_menu" name="Scans" parent="security_scan_menu_root"
                  action="security_scan_action" sequence="10"/>
    </data>
</odoo>